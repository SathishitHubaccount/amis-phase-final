CORRECTION NEEDED IN DEMAND_INTELLIGENCE_COMPLETE_GUIDE.docx

Section 4, Data Source 2: Current Inventory

WRONG (current version):
2. Current Inventory
SQL Query:
SELECT current_stock, safety_stock, warehouse_capacity
FROM inventory
WHERE product_id = 'PROD-A';
Returns: Current inventory levels

CORRECT (should be):
2. Current Inventory (from sample_data.py)
Python Function Call:
inventory = get_current_inventory(product_id)

Returns:
• current_stock: 1,850 units
• safety_stock: 300 units  
• warehouse_capacity: 5,000 units
• avg_daily_consumption: 142 units
• days_of_supply: 13 days
• incoming_orders: [Order 1, Order 2]
• unit_holding_cost: $2.30/unit/year
• unit_stockout_cost: $45/unit

Note: This data comes from Python functions (sample_data.py), NOT database tables.
In the future, this can be migrated to database (similar to historical_demand_data).

REASON:
The code in tools/forecasting.py shows:
from data.sample_data import get_current_inventory, get_production_capacity, get_product_info

Only historical_demand and market_context come from database_queries.py
